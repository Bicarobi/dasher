import { BreakEmailPipe } from './break-email.pipe';

describe('BreakEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
